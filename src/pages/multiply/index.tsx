import MultiplyBody from './body';
import Header from './header';

const MultiplyPage = () => {
  return (
    <div className="flex h-full flex-col" id="container">
      <Header />
      <MultiplyBody />
    </div>
  );
};

export default MultiplyPage;
