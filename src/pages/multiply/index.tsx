import MultiplyBody from './body';
import Header from './header';

const MultiplyPage = () => {
  return (
    <div id="container">
      <Header />
      <MultiplyBody />
    </div>
  );
};

export default MultiplyPage;
